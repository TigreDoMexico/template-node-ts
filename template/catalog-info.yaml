apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: ${{values.name | dump}}
  {%- if values.description %}
  description: ${{values.description | dump}}
  {%- endif %}
  annotations:
    github.com/project-slug: TigreDoMexico/${{values.name | dump}}
spec:
  type: service
  owner: user:TigreDoMexico
  lifecycle: project
  system: ${{values.system | dump}}